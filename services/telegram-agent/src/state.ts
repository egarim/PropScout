// Last list shown per chat — replying with a number opens that property.
// Shared by the chat flow (index.ts) and the alert digest (alerts.ts).
export const pendingResults = new Map<number, any[]>();
